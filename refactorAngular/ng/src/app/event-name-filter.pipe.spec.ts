import { EventNameFilterPipe } from './event-name-filter.pipe';

describe('EventNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EventNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});

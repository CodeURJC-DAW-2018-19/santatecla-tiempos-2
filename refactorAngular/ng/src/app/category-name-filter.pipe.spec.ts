import { CategoryNameFilterPipe } from './category-name-filter.pipe';

describe('CategoryNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
